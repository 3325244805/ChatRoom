<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.chatsys.mapper.UserMapper">

    <select id="findByUsername" parameterType="String" resultType="com.example.chatsys.entity.User">
        SELECT * FROM users WHERE username = #{username}
    </select>

    <select id="findByEmail" parameterType="java.lang.String" resultType="com.example.chatsys.entity.User">
        SELECT id, username, email, avatar_url as avatarUrl, nickname
        FROM users
        WHERE email = #{email} AND status = 1
    </select>


    <select id="selectByEmail" parameterType="java.lang.String" resultType="com.example.chatsys.dto.SearchResult">
        SELECT id, username, email, 1 as type
        FROM users
        WHERE email = #{email} AND status = 1
    </select>


    <select id="selectById" parameterType="java.lang.Long" resultType="com.example.chatsys.entity.User">
        SELECT id, username, email, avatar_url as avatarUrl, nickname
        FROM users
        WHERE id = #{id} AND status = 1
    </select>

    <insert id="insert" parameterType="com.example.chatsys.entity.User">
        INSERT INTO users(username, email, password_hash, nickname, status)
        VALUES(#{username}, #{email}, #{passwordHash}, #{nickname}, 1)
    </insert>




    <update id="updateLastLogin" parameterType="com.example.chatsys.entity.User">
        UPDATE users
        SET last_login_at = #{lastLoginAt},
            updated_at = CURRENT_TIMESTAMP
        WHERE id = #{id}
    </update>
</mapper>